\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[node distance=1.35cm and 1.55cm]
    \node[cmsstore] (content) {Contenido\\estructurado};
    \node[cmscontainer, right=of content] (model) {Modelo de página\\secciones ordenadas};
    \node[cmssystem, right=of model] (renderer) {Renderizador\\de páginas};
    \node[cmscontainer, right=of renderer] (visual) {Bloques visuales\\reutilizables};
    \node[cmssystem, right=of visual] (page) {Página final\\del sitio};

    \node[cmsnote, below=1.15cm of visual, xshift=-2.8cm] (block1) {Texto\\informativo};
    \node[cmsnote, right=0.4cm of block1] (block2) {Grillas\\y listas};
    \node[cmsnote, right=0.4cm of block2] (block3) {FAQ y\\contacto};
    \node[cmsnote, right=0.4cm of block3] (block4) {Categorías\\y puntuación};

    \draw[cmsarrow] (content) -- node[above, font=\scriptsize, align=center] {provee\\datos} (model);
    \draw[cmsarrow] (model) -- node[above, font=\scriptsize, align=center] {define\\orden} (renderer);
    \draw[cmsarrow] (renderer) -- node[above, font=\scriptsize, align=center] {selecciona\\bloques} (visual);
    \draw[cmsarrow] (visual) -- node[above, font=\scriptsize, align=center] {construye\\vista} (page);

    \draw[cmsarrow, dashed] (block1) -- (visual);
    \draw[cmsarrow, dashed] (block2) -- (visual);
    \draw[cmsarrow, dashed] (block3) -- (visual);
    \draw[cmsarrow, dashed] (block4) -- (visual);
\end{tikzpicture}%
}
\caption[Composición dinámica de páginas informativas]{Composición dinámica de páginas informativas mediante bloques reutilizables}
\label{fig:cms_composicion_paginas}
\end{figure}
